{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "buildCommand": "npm install --prefix backend && npm install --prefix frontend && npm run build",
    "startCommand": "node backend/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
